using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HatcheryManager : MonoBehaviour
{
    /// <summary>
    /// This is to manage the hatchery; players can feed the hatchery to influence what kind of egg it will drop (mammalian, avian, aquatic, reptilian)
    /// also influences rarity chances
    /// </summary>
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
